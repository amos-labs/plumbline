node_modules/
# tsc output is not committed, but the bundled action artifact (dist/index.js) is.
dist/*
!dist/index.js
.env
# Semantic-review verdict cache (opt-in, keyed by diff_sha256) — a local
# optimization, not source of truth.
.plumbline/cache/
.proofgate/cache/

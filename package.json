{
  "name": "proofgate",
  "version": "0.1.0",
  "description": "Proof-carrying gate for AI agent work: structured receipts, deterministic shape checks, LLM semantic review, and failure-capsule rework loops.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "proofgate": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates",
    "action.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "node --test dist/test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
